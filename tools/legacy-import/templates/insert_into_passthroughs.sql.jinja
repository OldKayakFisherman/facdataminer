INSERT INTO census_passthroughs
(
    DBKEY,
    AUDITYEAR,
    ELECAUDITSID,
    PASSTHROUGHNAME,
    PASSTHROUGHID
)
VALUES(
    {{DBKEY}},--DBKEY
    {{AUDITYEAR}},--AUDITYEAR
    {% if ELECAUDITSID  is ne '' %}{{ELECAUDITSID}}{% else %}NULL{% endif %},--ELECAUDITSID
    {% if PASSTHROUGHNAME  is ne '' %}'{{PASSTHROUGHNAME|replace("'","''")|safe}}'{% else %}NULL{% endif %},--PASSTHROUGHNAME
    {% if PASSTHROUGHID  is ne '' %}'{{PASSTHROUGHID|replace("'","''")|safe}}'{% else %}NULL{% endif %}--PASSTHROUGHID
);



